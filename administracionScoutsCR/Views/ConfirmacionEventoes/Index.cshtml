@model IEnumerable<administracionScoutsCR.Models.ConfirmacionEvento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asistencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEventoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asistencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEventoNavigation.IdEvento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdConfirmacionEvento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdConfirmacionEvento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdConfirmacionEvento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
