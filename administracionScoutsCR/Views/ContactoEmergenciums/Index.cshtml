@model IEnumerable<administracionScoutsCR.Models.ContactoEmergencium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parentesco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroContacto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Parentesco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroContacto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdContactoEmergencia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdContactoEmergencia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdContactoEmergencia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
